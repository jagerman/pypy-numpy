# Where to move the built wheels, i.e. the web-accessible PyPI repository
repo = 'trusty-pypi'
# pypy binary versions (keys) and pypy wheel suffixes (values) for each pypy
# version to build for:
pypy = {
    'pypy2-v5.8.0-linux64': '-pp258-pypy_41-linux_x86_64.whl',
    'pypy2-v5.9.0-linux64': '-pp259-pypy_41-linux_x86_64.whl'
}
